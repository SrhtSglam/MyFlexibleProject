@* @model List<Category>

 <div class="list-group">
    <a href="/" class="list-group-item list-group-item-action">Tüm Kategoriler</a>

    @foreach (var item in Model)
    {
        <a asp-controller="Shop" 
        asp-action="List" 
        asp-route-category="@item.Url" 
        class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == item.Url?"active":"")">
            @item.Name
        </a>
    }
</div>    *@

@model List<Category>
    @foreach (var item in Model)
    {
        <li><a asp-controller="Shop" asp-action="List" asp-route-category="@item.Url" class="nav-link rounded @(ViewBag.SelectedCategory == item.Url?"active":"")">@item.Name</a></li>
    }